定義一個字串陣列words和一個字元x，然後提示使用者輸入words，並存入 words陣列中，再提示使用者輸入要搜尋的單字，並存入變數 x 中，尋找包含 變數x 的所有words，並將它們存在 result 中，如果result是空值，輸出NULL，否則輸出符合的words。
然後用FindWords 方法尋找包含指定字元的words輸出結果，在FindWords 方法中List<int> 為暫時存符合條件的words，用for迴圈包含if else函數，如果包含變數 x則加到列表中，再將列表轉換為整數陣列傳回。

